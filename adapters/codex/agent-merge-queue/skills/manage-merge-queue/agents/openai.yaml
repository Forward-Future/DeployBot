interface:
  display_name: "Manage Merge Queue"
  short_description: "Review, queue, and land pull requests safely"
  default_prompt: "Use $manage-merge-queue to review and enqueue my pull request, then coordinate the merge queue."
dependencies:
  tools:
    - type: "mcp"
      value: "deploybot"
      description: "Durable GitHub merge queue operations"
